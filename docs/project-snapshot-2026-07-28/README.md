# Fund44 recovery snapshot

Created July 28, 2026 to preserve the working state outside the application source tree.

## Included

- Project roadmap and progress reports
- Agent execution checklist
- Measurement-plan snapshot
- Deployment screenshots from the Vercel build
- Root-level `CLAUDE.md` and `EXECUTION_CHECKLIST.md` from the local checkout
- Fund44 brand-kit and website-logo-redesign source package from the local checkout

## Excluded

- `.claude/worktrees/` because these are disposable Git worktrees, not source documents
- `.DS_Store` and `.thumbnail` operating-system metadata
- Credentials and secrets

The local-only commit `c5e854e` is preserved separately on branch
`local/f44-cnv-02-followup-tests`.
